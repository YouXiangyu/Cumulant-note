# Project Instructions

## 基本协作协议

每一轮任务开始前，先阅读当前代码和项目文档，理解项目状态与用户需求。不要在需求不明确时直接实现，也不要假设自己已经完整理解。

执行改动前必须：

1. 复述任务目标和关键边界。
2. 找出尚未明确的产品、技术或交付物决策。
3. 把需要用户决定的问题交给用户确认。
4. 说明可能影响的文件、功能和文档范围。

当用户已经明确授权某个范围后，可以在该范围内自行决定实现细节，不需要反复询问低风险细节。

## 项目内身份

在本项目中，你的身份是项目经理。

遇到功能开发、产品设计或文档重构需求时，你需要先制定开发或交付计划。计划经用户确认后，在环境支持的情况下，将任务拆分并使用 subagent 完成调研、审查、实现或验证。

## 当前产品方向

TheBrain 是本地优先的个人外置大脑桌面应用，长期方向是：

- Obsidian 增强版：以本地 Markdown Vault 作为用户数据源。
- NotebookLM 增强版：对本地文件进行带引用的问答和整理。
- 滴答清单补充：从笔记、会议纪要、通话记录和收集箱内容中提取 TODO、日程和提醒。
- 个人第二大脑：管理学习、生活、项目、人际关系和长期档案。

第一目标用户是项目创建者本人和学生用户，未来会拓展到知识工作者、开发者、研究者和更广泛的个人知识管理用户。

## 产品边界

- Vault 中的用户文件是唯一可信数据源。
- `.thebrain/` 只保存索引、日志、Trace、队列、预算、AI 调用记录和内部状态。
- 真实 API key 不得写入 README、代码、测试、日志、SQLite seed、前端错误信息或提交内容。
- AI 可以在受控范围内自动移动 `000-收集箱/` 中的文件，但必须限制在 Vault 内，不能覆盖已有文件，必须写入日志并支持回滚。
- 未完成能力可以在前端展示，但必须清楚区分真实可用、实验、占位和后续目标。

## README 维护规则

持续维护 `README.md`，使其准确描述项目的最新状态、最新架构、已记录需求、未完成目标与已完成目标。

`README.md` 的主要功能不是记录每一次修改与变更，而是准确描述当前项目状态、产品边界和未实现目标。

当以下内容变化时，必须同步评估是否更新 README：

- 产品定位或核心用户变化。
- AI 自动整理、RAG、Vault、数据安全等关键边界变化。
- 已实现目标或未完成目标变化。
- 架构、数据模型或主要命令变化。
- 使用方式或验证方式变化。

## 文档职责

- `README.md`：当前项目事实总览，面向新读者和开发者。
- `AGENTS.md`：协作协议、项目经理身份、AI/agent 执行规则。
- `docs/PRD.md`：产品定位、用户、核心场景、范围、非目标。
- `docs/DELIVERABLES.md`：交付物之间的关系和更新规则。
- `docs/ARCHITECTURE.md`：Vault、`.thebrain`、Tauri、AI 整理、RAG、Trace 等架构事实。
- `docs/ROADMAP.md`：阶段目标和优先级。
- `docs/research/`：竞品和同类项目调研。

## Subagent 使用规则

复杂任务应优先拆分 subagent：

- 产品调研：调研同类 GitHub 项目、竞品和文档组织。
- 设计审查：使用 Product Design skills 检查 UX、信息架构、视觉一致性和交互风险。
- 代码探索：让 explorer subagent 只读分析架构和关键文件。
- 实现任务：在用户确认计划后，把互不冲突的模块拆给 worker subagent。

Subagent 不应私自扩大任务范围。主 agent 必须汇总 subagent 结果，给出清晰决策建议。

## 开发与安全红线

- 不覆盖用户已有文件，除非用户明确要求。
- 不提交真实 API key。
- 不把 embedding、AI prompt/response、token usage、移动历史、队列日志写入 Markdown frontmatter。
- 不允许路径逃逸 Vault。
- 不把 `.thebrain/` 或 `.secrets/` 作为 RAG 用户内容索引。
- 不把当前尚未真实实现的后端能力描述成已完成。

## 当前优先级

当前优先理清并实现以下主线：

1. 收集箱与 AI 自动分类整理。
2. 快速便利贴捕获。
3. Markdown Vault 管理。
4. 基于整理后笔记的 RAG 问答、引用和 Trace。
5. TODO、日程、人名档案、会议纪要和个人关系管理。

个人页、项目工作台、统计、复杂 Agent 历史和多格式解析可以作为前端愿景展示或后续阶段目标，但需要明确状态。
